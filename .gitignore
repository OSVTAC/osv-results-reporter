__pycache__/
*.egg-info/
# The default output directory.
_build/
.DS_Store
.python-version
# Ignore binary files like PDF's and .tar.gz files so we don't accidentally
# store them in source control.
*.pdf
*.tar.gz
